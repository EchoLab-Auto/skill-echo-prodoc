---
name: prodoc-format
description: ProDoc 文档格式规范 —— 基于 Markdown 的文档组织约定，定义文档图模型（每个 .md = 图上一个框）、框架参数区语法、link 连线声明、层级自动布局、文档内分块，以及 prodoc-flow 流程图语法
---

# ProDoc 文档格式规范

ProDoc 是一种基于 Markdown 的文档组织约定，定义了如何将一组 Markdown 文件组织成可供 ProDoc 渲染器识别和渲染的文档结构。

> 当前模型为**文档图**（DocGraph）：目录即图，文件即框，连线表达导航。旧的「文档树」模型（`order` 排序、`index.md` 目录节点）及其 `edit` 命令已移除。

## 一、文件格式

所有 ProDoc 文档必须是 **`.md` 扩展名** 的 Markdown 文件，使用 UTF-8 编码。

## 二、核心模型：文档图

ProDoc 把一个文档目录（文档群）视为**一张图**，而非目录树：

- **每个 `.md` 文件 = 图上的一个框**（box），目录层级只影响路径，不构成父子关系
- 框的属性（标识、标题、位置、尺寸）由文件最前方的**框架参数区**声明
- 框间用 `link` 参数声明的有向连线表达导航关系，连线结构决定框的**层级深度**
- 正文的 **H2 标题**自动提取为**文档内分块**，悬停框时展开为可跳转面板
- 文档群被整体渲染为一张可交互画布，点框即跳转到该文档的正文

## 三、框架参数区

### 3.1 语法

每个 `.md` 文件的**最前方**（文件第 0 字节起）是由两个 `---` 行夹住的参数区：

```
---
id: getting-started
title: 快速开始
x: 40
y: 64
---

# 正文从这里开始……
```

- 第一行必须是单独的 `---`；之后是若干 `key: value` 参数行；以一行单独的 `---` 结束
- 结束行之后的所有内容即**正文**

### 3.2 剥离规则（渲染不可见）

参数区**仅供框架消费**，正文渲染时完全剥离；读者在查看器中看到的一切（正文、目录、链接）均基于剥离后的正文。

### 3.3 值类型

| 类型 | 示例 | 说明 |
|------|------|------|
| 整数 | `x: 40` | `^-?\d+$` |
| 浮点数 | `scale: 1.5` | `^-?\d*\.\d+$` |
| 布尔值 | `draft: true` | `true` / `false` |
| 内联列表 | `link: [guide, api]` | 方括号包裹、逗号分隔；成员继续按本表规则解析 |
| 引号字符串 | `title: "快速开始"` | 双引号或单引号包裹，取内部内容 |
| 裸字符串 | `author: 张三` | 以上都不匹配时，原样作为字符串 |

### 3.4 容错

- 文件**不以 `---` 开头**：视为无参数区，整个文件按普通正文处理
- 文件以 `---` 开头但**找不到闭合的第二个 `---`**：视为无参数区，整个文件（含开头的 `---`）按正文处理——宁可显示原文，也不吞掉内容
- 参数区内**无法识别的行**：跳过，不中断解析

## 四、框的属性

### 4.1 框架保留字段

| 字段 | 类型 | 必填 | 缺省行为 |
|------|------|------|----------|
| `id` | `string` | 否 | 取文件相对路径去掉 `.md`，如 `guide/getting-started` |
| `title` | `string` | 否 | 取正文第一个 H1；再缺省取 `id` |
| `x` | `number` | 否 | 缺省时按连线层级分层自动布局（见 §六） |
| `y` | `number` | 否 | 同上 |
| `w` | `number` | 否 | `220` |
| `h` | `number` | 否 | `96` |
| `link` | `string[]` | 否 | 当前文档指向其他文档的连线（见 §五） |

`title` 缺省回退链：`参数区 title → 正文第一个 H1 → id`。

### 4.2 自定义字段

参数区中除保留字段外的任意字段都会被解析并存入框的 `attrs` 对象，供框架或上层工具扩展使用，不影响渲染。

### 4.3 文档内分块

正文的 **H2 标题**（`## 分块名`）会被自动提取为框的 `blocks`：

- 仅当文档包含 **2 个及以上 H2** 时提取；代码围栏内的 `##` 不视为标题
- 悬停（或键盘聚焦）框时从框底弹出分块面板；贴近画布底边的框向上弹出
- 面板最多展示 6 个条目，超出折叠为 `+N 更多分块…`
- 点击面板条目打开正文并**滚动到对应标题**

## 五、连线关系（link）

`link` 直接声明当前文档指向其他文档的有向连线，方向为「当前文档 → 目标文档」：

```yaml
---
id: project
link: [guide, api]
---
```

### 5.1 标签

条目可携带标签，`|` 之后的文字显示在连线中点：

```yaml
link: ["guide | 使用指南", api]
```

### 5.2 连接边

条目可追加第三段指定**连接边**——`源边>目标边`，`t/r/b/l` 分别表示上/右/下/左，`_` 表示该端保持自动：

```yaml
link: ["guide | 使用指南 | b>l", "api | r>t", "note | t>_"]
```

缺省时按两框主导方向自动选边；标签与连接边按模式识别、互不占位（`user | r>l` 即无标签仅指定边）。在图编辑模式下选中连线后拖动端点手柄即可可视化调整，无需手写。

### 5.3 解析与容错

- 引用优先按文档 `id` 解析，也可写相对文档根的路径（`.md` 后缀可省略）
- 重复声明同一连线时自动去重
- 引用不存在的文档或连线自身时记录警告，不生成连线，也不中断渲染
- `link` 属于框架保留字段，不进入 `attrs`

### 5.4 连线渲染

连线渲染为**贝塞尔曲线箭头**：端点取所连边的**边缘中点**，控制点沿两端边的外法线布置——曲线在两端始终与边线法线相切；标签文字显示在连线中点。

## 六、层级深度与分层自动布局

每个框根据 `link` 连线结构计算**层级深度**：根框（无入边）深度为 0，其余框深度 = 父框最大深度 + 1；环上的回边不参与深度贡献。

缺省 `x` / `y` 的框按深度**分层自动排布**：

- 深度相同的框同属一层，顶层为根框，逐层向下
- 层内按父框平均横坐标（重心）排序，减少连线交叉；无父框的框保持文件顺序排在右侧
- 每层从 `(48, 48)` 起排布，横向步距 `框宽 + 64`，层间纵向间距 `72`；层过宽时按 `⌈√自动框数⌉` 列换行
- 显式给出坐标的框不受影响；只缺一个坐标轴时仅补写缺失轴
- `view` 模式会把自动计算的坐标**写回**参数区，使布局固定下来

画布头部的「🧭 分层重排」可忽略文件坐标临时重排全图（不写文件）；「↩ 恢复坐标」还原。

## 七、流程图语法（prodoc-flow）

ProDoc 支持用 ` ```prodoc-flow ` 代码块在文档中定义**流程图**。渲染时该代码块呈现为**可交互的流程画布**（拖拽平移、Ctrl+滚轮缩放、一键适配），流程中的节点可以关联文档，点击节点即跳转。

### 7.1 方向声明

第一个有效行必须是方向声明：

```
graph LR
```

| 方向 | 含义 |
|------|------|
| `LR` | 从左到右（默认） |
| `RL` | 从右到左 |
| `TB` | 从上到下 |
| `BT` | 从下到上 |

缺少方向声明时按 `LR` 处理并记录一条解析警告。

### 7.2 节点

| 语法 | 形状 |
|------|------|
| `A[文本]` | 矩形 |
| `A[/文本/]` | 圆角矩形 |
| `A(文本)` | 体育场形 |
| `A{文本}` | 菱形 |
| `A` | 裸 id 引用（自动登记为矩形，显示文本为 id） |

- 节点 id 为裸标识符（字母/数字/下划线/连字符，字母开头），文本可含空格与中文
- 同一 id 的完整声明后出现时会**合并升级**先前的裸引用（后者优先）
- 独立一行的 `A[文本]` 是节点声明；节点也可在边行中内联声明

### 7.3 边与标签

```
A --> B          %% 基本边
A -->|是| B      %% 带标签的边
A --> B --> C    %% 链式（等价于 A --> B、B --> C）
```

完全相同的边（起点 + 终点 + 标签）重复声明时只保留一条。

### 7.4 文档链接

节点文本中以竖线附加文档路径，即把节点变为**可点击的文档链接**：

```
A[首页|/index.md] --> B[用户中心|/api/user.md]
```

- 路径必须以 `/`、`./` 或 `../` 开头并以 `.md` 结尾，相对文档根目录
- 带链接的节点渲染为主色描边 + ↗ 图标，点击（或 Enter/Space）在查看器内跳转

### 7.5 注释

`%%` 之后的内容为注释（可整行或位于行尾）。

### 7.6 容错规则

- 无法识别的行不会中断解析，而是被跳过并记录到解析警告中
- 整个代码块没有任何有效节点时，按普通代码块原样显示源码
- 建议单张流程图节点数控制在 30 个以内，以保证自动布局的可读性

### 7.7 手动排版（`@ x, y`）

节点声明行尾可附加画布坐标（节点左上角，px），显式坐标的节点不再参与自动排布：

```
graph LR
  A[开始] @ 48, 96
  B[处理] --> C[结束]   %% 未标注的节点照常自动布局
```

- 坐标为最终视觉坐标（与方向镜像无关）；只给部分节点标注时，其余节点自动分层排布
- 边链行尾的标注绑定到**链尾节点**：`A --> B @ 40, 64` 设定的是 B
- 查看器中**直接拖拽节点**即可调整排版：松手后坐标以 `@ x, y` 形式自动写回该代码块（仅出现在边链中的节点会在块尾追加一行坐标声明），无需手写

## 八、渲染与交互速览

- **画布交互**：拖拽平移、Ctrl/Cmd+滚轮缩放、一键适配；悬停框高亮上下游链路、展开分块面板
- **图编辑模式**：头部「🛠 编辑图」开启——拖拽框调整位置（写回 `x`/`y`）、连接点拖出创建连线、点选连线调整连接边或删除；「✓ 完成」退出恢复纯浏览
- **浏览编辑一体化**：正文视图「✏️ 编辑」切换为 Markdown 编辑器（编辑原始完整文件），Ctrl/Cmd+S 保存写回源文件
- **文档热更新**：服务运行期间新增、修改、删除 `.md` 文件，页面原地更新（不刷新）
- **地址栏 hash** 同步当前文档路径，刷新/分享可直达

完整交互逻辑的流程图见 `document/develop/interaction-design/interaction-design.md`；权威规则定义见 `document/develop/prodoc-rule/prodoc.md`。

## 九、完整示例

```
my-docs/
├── index.md
├── guide.md
└── note.md
```

`index.md`：

```markdown
---
id: home
title: 首页
x: 40
y: 40
link: ["guide | 指南"]
---

# 首页

这是文档群的入口。[前往指南](./guide.md)
```

`guide.md`（含分块，悬停框可展开面板）：

````markdown
---
id: guide
title: 使用指南
x: 400
y: 40
---

# 使用指南

## 安装

## 配置

```prodoc-flow
graph LR
  Home[首页|/index.md] --> Guide[使用指南|/guide.md]
```
````

`note.md`（无框架参数区，分层自动布局）：

```markdown
# 无参数区文档

这个文件没有参数区，会自动获得一个框，标题取自 H1。
```

## 十、内容规范建议

1. **标题层级**：每个文件以单一 H1（`#`）开头作为页面标题，正文从 H2（`##`）开始——H2 同时是画布分块面板的条目，命名应简洁可导航
2. **参数区最小化**：`id`/`title` 多数情况可省略（回退链足够好用）；`x`/`y` 交给自动布局与画布拖拽写回，不必手填
3. **连线即导航**：用 `link` 表达主要阅读路径，标签只在需要说明关系时添加
4. **文件名**：使用小写字母 + 连字符命名（如 `getting-started.md`）
5. **相对链接**：使用相对路径链接到同文档群内的其他 `.md` 文件，查看器内可继续跳转
6. **代码块**：始终为代码块指定语言标识
7. **流程图**：单张图节点数 < 30；优先用文档链接把流程图变成可钻取的导航地图

## 十一、验证方法

```bash
# 在 DocRenderer 仓库内启动文档查看服务器
npm run view -- ./your-doc-directory

# 或使用已安装的 CLI
echo-prodoc view ./your-doc-directory
```

验证清单：

- [ ] 所有 `.md` 文件使用 UTF-8 编码
- [ ] 参数区格式正确（两个 `---` 行夹住，键值行合法）
- [ ] 画布显示全部文件的框，无坐标框按连线层级分层排布
- [ ] `link` 连线显示为曲线箭头，标签/连接边符合声明
- [ ] 悬停框展开分块面板（H2 ≥ 2 时），点击条目滚动到对应标题
- [ ] 图编辑模式下拖框、增删连线、调整连接边均写回源文件
- [ ] 修改磁盘文件后页面原地更新（热更新）
- [ ] `prodoc-flow` 块首行为 `graph LR|RL|TB|BT` 方向声明
- [ ] 流程图渲染为交互画布（而非普通代码块），带 ↗ 的节点可点击跳转
